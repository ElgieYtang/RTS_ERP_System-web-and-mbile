# Leave empty in local Vite so requests go to `/api` and the Vite proxy hits Laravel.
# For production builds served separately from the API, set the full API base:
# VITE_API_URL=https://api.your-domain.com/api
VITE_API_URL=
